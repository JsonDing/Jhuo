<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">@android:color/white</item>
        <item name="titleTextColor">@android:color/white</item>
    </style>
    <!-- Button -->
    <style name="BlockButton">
        <item name="android:layout_width">@dimen/block_button_width</item>
        <item name="android:layout_height">@dimen/block_button_height</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="OrangeButton">
        <item name="colorButtonNormal">@color/a6</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- CheckBox -->

    <style name="OrangeCheckBox">
        <item name="colorControlActivated">@color/md_amber_500</item>
        <item name="colorControlNormal">@color/md_grey_500</item>
    </style>

    <!-- RadioButton -->

    <style name="GreenRadioButton">
        <item name="colorControlActivated">@color/md_green_500</item>
        <item name="colorControlNormal">@color/md_grey_500</item>
    </style>

    <!-- Switch -->

    <style name="OrangeSwitch">
        <item name="colorControlActivated">@color/a6</item>
        <item name="colorControlNormal">@color/md_grey_500</item>
    </style>

    <style name="MyCheckBox">
        <item name="android:button">@drawable/check_box_style</item>
    </style>

    //底部弹出菜单
    <style name="BottomDialog" parent="@android:style/Theme.Dialog">
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- Dialog进入及退出动画 -->
        <item name="android:windowAnimationStyle">@style/DialogAnimationBottom</item>
    </style>
    <!-- ActionSheet进出动画 -->
    <style name="DialogAnimationBottom" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/dialog_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_out</item>
    </style>

    //中间弹出菜单
    <style name="CenterDialog" parent="@android:style/Theme.Dialog">
        <!--设置dialog的背景-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--设置Dialog的windowFrame框为无-->
        <item name="android:windowFrame">@null</item>
        <!--设置无标题-->
        <item name="android:windowNoTitle">true</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否半透明-->
        <item name="android:windowIsTranslucent">true</item>
        <!--设置窗口内容不覆盖-->
        <item name="android:windowContentOverlay">@null</item>
        <!--设置动画，在这里使用让它继承系统的Animation.Dialog-->
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <!--背景是否模糊显示-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>
    <style name="DialogAnimationCenter" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/fade_in</item>
        <item name="android:windowExitAnimation">@anim/fade_out</item>
    </style>


    <style name="StyleProgressBar" parent="Widget.AppCompat.ProgressBar.Horizontal">

        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">
            @android:drawable/progress_indeterminate_horizontal
        </item>
        <item name="android:progressDrawable">@drawable/bg_grade_progress</item>

    </style>

</resources>
